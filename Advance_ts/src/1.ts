// type Direction = 'east' | 'west' | 'south' | 'north';

/*
//! this is numaric enum

enum Direction {
  East,
  Weast,
  North,
  South,
}
 */

enum Direction {
  East = 'east',
  West = 'west',
  North = 'north',
  South = 'south',
}

let a: Direction = Direction.East;
