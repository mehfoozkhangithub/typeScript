const add = (a, b) => {
    return a + b;
};
add(1, 2); //Okay
//add(1); //error need the second args
//add(5,"hello") // error
// sec eg :-
const log = (message) => {
    console.log(" message:", message);
};
//# sourceMappingURL=2.js.map